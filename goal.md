# OpenClaw — Project Notes

Catch-all for tools, scripts, and experiments related to the OpenClaw platform itself (not one of our own apps/projects).

## tools/
- `goalee-task-add.sh` / `goalee-task-update.sh` / `sync-goals.py` — goal DB management (legacy, pending redesign)
- `get-secret.sh` — Keychain helper
- `publish-plugin.py` — publish OpenClaw plugins
- `fix-announce-queue.sh` — OpenClaw announce queue fix
- `simple-sdk-test.mjs` / `test-sdk-import.mjs` — SDK exploration
- `tool-policy-draft.json` — tool policy draft (pending)
- `cleanup-screenshots.sh` — midnight cron: deletes screenshots older than yesterday
